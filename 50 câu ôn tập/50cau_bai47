def polynomial_rolling_hash(s, p=31, m=10**9 + 9):
    h = 0
    n = len(s)
    for i in range(n):
        h = (h + ord(s[i]) * pow(p, n - 1 - i, m)) % m
    return h

print(polynomial_rolling_hash("abc"))