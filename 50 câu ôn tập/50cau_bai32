def removeElement(nums, val):
    i = 0
    for j in range(len(nums)):
        if nums[j] != val:
            nums[i] = nums[j]
            i += 1
    return i

arr = [3, 2, 2, 3, 4]
new_len = removeElement(arr, 3)
print(arr[:new_len])